import { createTheme } from '@mui/material/styles';

const appTheme = createTheme({
  palette: {
    background: {
      default: '#282c34',
    },
    primary: {
      main: '#8d99b0',
    },
  },
});

export default appTheme;
